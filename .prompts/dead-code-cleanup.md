角色： 你是一位精通代码重构的资深工程师。

任务： 对当前代码进行一次深度清理和 Code Review。这段代码经过多轮 AI 迭代，目前存在逻辑冗余，请在完全确保程序最终执行的结果不变的前提下，执行以下优化：

剪枝： 删除所有未使用的变量、配置参数、无用函数以及不再调用的 CSS/样式。

逻辑精简： 简化复杂的 if-else 嵌套，移除冗余的校验逻辑，合并功能重合的代码块。

可读性修复： 重新组织函数顺序，理顺代码流向，确保逻辑清晰易懂。

无损压缩： 确保重构后的代码功能与原版 100% 一致。

输出要求： 直接给出重构后干净整洁的代码，并简要列出你删除或优化了哪些主要部分。
